struct IntNode{
    int value;
    IntNode* next;
};

struct IntStack{

    //push method
    void push(int value){
        IntNode* newNode = new IntNode;
        newNode->value = newVal;
        newNode->next = top.
        top = newNode;
    }

    //pop method
    void pop(){

    }
    
};